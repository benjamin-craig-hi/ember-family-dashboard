# OLLAMA_HOST — where the LLM lives. Defaults to localhost.
# Point this at a GPU workstation on your LAN to offload the model
# (the kiosk is CPU-only and too slow for a good model).
OLLAMA_HOST=http://localhost:11434

# OLLAMA_MODEL — the Ollama model to use.
OLLAMA_MODEL=qwen3:8b-q8_0
