# Runtime data (notes/chores/calendar) — never commit family data
data/
calendar.json
notes.json
chores.json
voice_config.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Wake-word training clips
wakeword-training/

# Env / secrets — never commit real credentials
.env
